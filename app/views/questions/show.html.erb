<h1>Страница вопроса</h1>

<p><%= link_to 'На главную', tests_path %></p>

<%= @question.title %>

<%= link_to 'редактировать', edit_test_question_path(@test, @question) %>
<%= link_to 'удалить', test_question_path(@test, @question), method: :delete, data: {confirm: 'Удалить вопрос?'} %>
